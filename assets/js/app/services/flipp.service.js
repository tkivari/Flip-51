define("App.Service.Flipp", function(module) {

    var BaseService = require("App.Service.Base");

    var FlippService = BaseService.extend({
        SearchProducts: function(query_string) {

        }
    });

    module.exports = FlippService;
});